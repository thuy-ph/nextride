// Shared with the static GitHub Pages build so the same park rules power
// both the local server proxy and the browser-only deployment.
export { PARKS, demoLiveData } from "../public/shared/parks.js";
